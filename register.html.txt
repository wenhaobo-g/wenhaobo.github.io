<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register | FunGames</title>
    <style>
        * { margin:0; padding:0; box-sizing:border-box; font-family:Arial; }
        body { background:#121212; color:white; display:grid; place-items:center; height:100vh; }
        .box { background:#1e1e1e; padding:2rem; border-radius:10px; width:100%; max-width:400px; }
        h2 { color:#4fc3f7; margin-bottom:1rem; text-align:center; }
        input { width:100%; padding:10px; margin:8px 0; border:none; border-radius:6px; }
        button { width:100%; padding:10px; background:#4fc3f7; color:white; border:none; border-radius:6px; margin-top:10px; cursor:pointer; }
        .link { text-align:center; margin-top:1rem; color:#aaa; }
        a { color:#4fc3f7; text-decoration:none; }
    </style>
</head>
<body>
    <div class="box">
        <h2>Register</h2>
        <input type="text" id="user" placeholder="Username">
        <input type="password" id="pass" placeholder="Password">
        <button onclick="register()">Register</button>
        <div class="link">Already have an account? <a href="login.html">Login</a></div>
    </div>

    <script>
        function register() {
            const user = document.getElementById('user').value;
            const pass = document.getElementById('pass').value;
            if (!user || !pass) return alert('Fill all fields');
            const accounts = JSON.parse(localStorage.getItem('accounts')) || [];
            accounts.push({ user, pass });
            localStorage.setItem('accounts', JSON.stringify(accounts));
            alert('Register success!');
            location.href = 'login.html';
        }
    </script>
</body>
</html>